# Roadmap

Authoritative detail lives in `.lovable/plan.md`.

- [x] M1 code work — permissions fix, PAR ratio, settings scrub, branding,
      SoD retargeted to lending, SaaS plan-limit gate removed.
- [ ] M1 close-out — owner verification pass in the running app (5 checks).
- [ ] M2 — lending-role access groups + ERP dead-code strip + RLS pass.
- [ ] M3 — dependency-checked drop of ERP schema, then linter hardening.
