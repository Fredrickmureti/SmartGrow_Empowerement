COMMENT ON COLUMN public.payslip_lines.source IS
  'Pack/engine-authored explanation jsonb. May include `bracket_breakdown`, `employee`/`employer` arrays, `rate`/`base`, `explanation`, and (Phase 4 P1.3) `input_ref`: discriminated provenance pointer of shape { kind, ...kind-specific fields } where kind is one of contract | salary_structure | payslip_input | variable_input | work_entry | attendance | leave_request | loan | garnishment | statutory_rule | retro | expense | reimbursement | benefit | termination_payout | override. Country-agnostic — the engine never branches on jurisdiction here. Phase 4 P4: navigable from the explainer via src/lib/payroll/payslipDrillDown.ts (see ADR 0046). Adding a new kind without updating that resolver fails the src/test/architecture/payslip-input-lineage.test.ts guard.';