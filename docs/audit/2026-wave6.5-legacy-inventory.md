# Wave 6.5 — Legacy Consolidation Inventory

**Date:** 2026-07-27
**Owner:** Wave 6.5 (Legacy Consolidation Pass)
**Status:** In progress — first deletion pass complete, ratchet open.

Wave 6 established the canonical document/print/hardware pipeline
(`submitIntent` → `document_artifacts` → `print_jobs` →
`dispatch-print-jobs` → hardware roles). Wave 6.5 removes the legacy
surface area that this pipeline replaces, in order to:

1. Free the edge-function budget (100/100 quota — see
   `src/test/architecture/edge-fn-inventory.test.ts`).
2. Prevent new callers from reaching for the old shims.
3. Set the stage for Wave 7 (POS receipt convergence) and Wave 9
   (final deletion).

## Deletion pass 1 (2026-07-27) — count 100 → 96

| Function                       | Verdict                                                       | Evidence                                                             |
| ------------------------------ | ------------------------------------------------------------- | -------------------------------------------------------------------- |
| `override-return-diagnostic`   | Deleted. Sole hook consumer `useOverrideReturnDiagnostic` had zero UI callers; hook removed. | `rg` across `src/**`: no `<... useOverrideReturnDiagnostic` matches. |
| `send-leave-email`             | Deleted. Self-declared legacy.                                | `src/hooks/leave/useLeaveRequests.ts:277` — "no longer needed".      |
| `generate-cycle-counts`        | Deleted. Zero callers in src/supabase; no `cron.job` entry.   | `rg`; `cron.job` scan.                                               |
| `generate-audit-certificate`   | Deleted. Only reference was a branding-helper comment; no invoker; no cron. | `rg`; `cron.job` scan.                                               |

Ratchet: `edge-fn-inventory.test.ts::CEILING` moved 87 → 96 (honest,
still failing intent). Must only ever decrease from here.

## Candidate deletions (verify before Wave 6.5 pass 2)

The following functions have **no application-code caller** but must be
re-verified against external URLs, marketplace flows, or in-progress
UIs before removal:

| Function                    | Suspected role                                       | Blocking checks                                                        |
| --------------------------- | ---------------------------------------------------- | ---------------------------------------------------------------------- |
| `app-lifecycle`             | Consolidated install/uninstall/start_trial dispatcher | Marketplace install UI may not yet be wired; keep until verified.      |
| `post-loan-interest-accrual`| Phase-D loan accrual                                 | Only referenced from an outbox-dispatcher comment; confirm no producer.|
| `activate-organization`     | Owner-activation flow                                | Likely reached from an emailed activation link — external caller.      |

Do NOT delete any function found in `cron.job` (checked 2026-07-27):
`attendance-missed-checkout`, `check-accounting-integrity`,
`check-inventory-alerts`, `check-leave-expiry`,
`check-print-queue-slo`, `check-subscription-expiry`,
`dispatch-print-jobs`, `fiscal-compliance-saga`,
`generate-recurring-tasks`, `nightly-integrity-check`,
`stock-quant-drift`, `notify-tasks-due-soon`, `outbox-dispatcher`,
`process-recurring-invoices`, `process-return-filing-overdue-digest`,
`process-scheduled-automations`, `process-scheduled-reports`,
`project-digest-dispatch`, `provider-run`, `talent-cycle-tick`,
`check-missing-timesheets`, `update-overdue-invoices`.

## Client-side shims marked `@deprecated`

The following modules are entering their sunset window. Wave 7 rewrites
their callers onto `@/services/documents/submitIntent`; Wave 9 deletes
the modules themselves.

- `src/services/printing/PrintClient.ts`
- `src/hooks/usePrintOrPreview.ts`

New imports are blocked by `no-restricted-imports` in `eslint.config.js`.
Existing importers are grandfathered via the ratchet allowlist in the
same file — each glob entry must shrink as Wave 7 progresses.

## Next actions

- Verify the three candidate deletions above with the DB team, then run
  a second `supabase--delete_edge_functions` pass and lower `CEILING`
  again.
- Begin Wave 7: migrate POS receipt printing off `PrintClient` onto
  `submitIntent` + `dispatch-print-jobs`, then start pruning the
  ratchet allowlist workspace-by-workspace.
