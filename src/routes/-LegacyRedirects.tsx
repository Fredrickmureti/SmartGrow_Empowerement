/**
 * Legacy redirect routes. Currently a no-op stub — return an empty array
 * if no legacy URL rewrites are needed.
 */
export function getLegacyRedirectRoutes(): React.ReactNode[] {
  return [];
}
