import { ScheduledReportsManager } from "@/components/studio/ScheduledReportsManager";

export default function StudioScheduling() {
  return (
    <div className="container mx-auto py-4 sm:py-6 px-3 sm:px-6">
      <ScheduledReportsManager />
    </div>
  );
}
