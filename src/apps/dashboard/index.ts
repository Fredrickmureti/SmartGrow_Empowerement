/**
 * Dashboard App Module — global Home / Overview workspace.
 *
 * Promotes the previously-special-cased "/dashboard" page to a regular
 * PlatformShell-hosted app so it shares one design foundation with every
 * other module. See `.lovable/plan.md` § P0.1.
 */
export { DashboardAppLayout, default } from "./DashboardAppLayout";
export { DASHBOARD_NAV } from "./nav";
