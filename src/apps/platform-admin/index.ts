/**
 * Platform Admin app package — the single import surface for the admin
 * persona. See `docs/design-system/audit/platform-admin.md` for the
 * interaction-classification rules every new admin page must follow.
 */
export { PlatformAdminAppLayout } from "./PlatformAdminAppLayout";
export * from "./scaffolds";
