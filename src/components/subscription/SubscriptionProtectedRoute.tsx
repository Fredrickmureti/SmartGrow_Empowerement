/**
 * @deprecated SaaS-era name. Use `InstitutionRoute` from
 * `@/components/auth/InstitutionRoute`. Kept as an alias while the
 * microfinance convergence migrates call sites.
 */
export { InstitutionRoute as SubscriptionProtectedRoute } from "@/components/auth/InstitutionRoute";
