/**
 * InactivityLockScreen
 * 
 * DEPRECATED: This component is no longer used.
 * The inactivity system now performs a full sign-out and redirects to /login,
 * where users can re-authenticate using password or PIN.
 * 
 * Kept as a stub for backward compatibility with any remaining imports.
 */

export function InactivityLockScreen() {
  return null;
}
